extends layout 

block content 
    h1 Author: #{author.fullName}

    p #{author.dateOFbirth}

    if authorBooks.length 
        dl 
            each book in authorBooks 
                dt 
                    a(href = book.url) #{book.title}

                dd #{book.summary}    
