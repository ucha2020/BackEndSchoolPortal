extends layout 

block content 
    h1= title 
    if book_list.length 
    ul 
    each book in book_list 
        li 
            a(href = book.url) #{book.title} 
            | (By #{book.author.fullName})
    else 
        p There are no books  . 